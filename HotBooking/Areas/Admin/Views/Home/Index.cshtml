@*@model IQueryable<InspirationItem>
    @{
        string strTitle = "Administrator Panel";
        ViewBag.Title = strTitle;
    }

    <div>
        <h2>@strTitle</h2>
        <div>
            <h3>Inspiration notes</h3>
            <div class="div-box">
                <a asp-area="Admin" asp-controller="InspirationItems" asp-action="Edit" asp-route-id="">Add new note</a>
            </div>
            @if (Model.Any())
            {
                <div>
                    @foreach (InspirationItem entity in Model)
                    {
                        <div>
                            <a asp-area="Admin" asp-controller="InspirationItems" asp-action="Edit" asp-route-id="@entity.Id">edit</a>
                            |
                            <form style="display: inline-block;" id="form-@entity.Id" asp-area="Admin" asp-controller="InspirationItems" asp-action="Delete" method="post">
                                <input type="hidden" name="id" value="@entity.Id">
                                <a href="#" onclick="document.getElementById('form-@entity.Id').submit();">delete</a>
                            </form>
                            |
                            <a asp-area="" asp-controller="Services" asp-action="Index" asp-route-id="@entity.Id">
                                @($"{entity.Title}")
                            </a>
                        </div>
                    }
                </div>
            }
        </div>
        <div class="div-box">
            <h3>Edit site pages</h3>
            <a asp-area="Admin" asp-controller="TextFields" asp-action="Edit" asp-route-codeWord="PageIndex">Главная</a>
            <a asp-area="Admin" asp-controller="TextFields" asp-action="Edit" asp-route-codeWord="PageServices">Inspiration</a>
            <a asp-area="Admin" asp-controller="TextFields" asp-action="Edit" asp-route-codeWord="PageContacts">Contacts</a>
        </div>
        <div class="div-box">
            <form asp-area="" asp-controller="Account" asp-action="Logout" method="post">
                <input type="submit" value="Exit" />
            </form>
        </div>
    </div>*@


<body>
    <br>
    <main class="container">
        <div class="starter-template text-center py-5 px-3">
            <h1>Admin Page</h1>
            <p class="lead">Use this page to access HotBooking site's database.</p>
        </div>       
    </main>
</body>
